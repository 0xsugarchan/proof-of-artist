# Optional: Sepolia HTTPS RPC (ENS + registry reads)
NEXT_PUBLIC_ETH_RPC_URL=

# Optional: deploy DemoAgentRegistry.sol on Sepolia and paste address here for on-chain rows
NEXT_PUBLIC_AGENT_REGISTRY_ADDRESS=

# Optional: curator chat via OpenAI (otherwise mock replies)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
